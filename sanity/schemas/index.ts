import project from "./project-schema";
import about from "./about-schema";
import home from "./home-schema";
import page from "./page-schema";

const schemas = [project, page, home];

export default schemas;
